{
  "name": "obsidian-mcp",
  "version": "0.4.1",
  "description": "MCP server for Obsidian vaults over the filesystem. Built to learn MCP internals.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "obsidian-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "gray-matter": "^4.0.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
